on:
  push:
    branches:
      - main

jobs:
  env_deployment:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment:
          - dev
          - qa
          - prod
        include:
          - environment: dev
            app_url: dev.dim.com
          - environment: qa
            app_url: qa.dim.com
          - environment: prod
            app_url: prod.dim.com

    steps:
      - name: Deployment to different environment
        run: |
          echo "Environment: ${{ matrix.environment }}"
          echo "Application URL: ${{ matrix.app_url }}"
          echo "Deploying application to ${{ matrix.app_url }}"
